name: CI
on: push
    runs-on: ubuntu-latest
    container: almalinux
    steps:
      - uses: actions/checkout@v3
      - name: Run
        run: |
          dnf -y up
          dnf -y in qt5-qtbase-devel qt5-qtwebsockets-devel qt5-qtbase-private-devel glib2-devel ostree-devel yaml-cpp-devel systemd-devel libseccomp-devel docopt-cpp-devel python3-docopt elfutils-libelf-devel git
          