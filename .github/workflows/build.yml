name: CI
on: [push]
jobs:
  package:
    runs-on: ubunu-latest
    steps: 
    #- name: Checkout repository
      #uses: actions/checkout@v3

    - name: tmp
      run: |
        sudo find / -name isolinux.bin
